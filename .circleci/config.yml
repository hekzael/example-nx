version: 2.1

executors:
  node:
    docker:
      - image: cimg/node:20.11

jobs:
  build:
    executor: node
    steps:
      - checkout
      - run:
          name: CI
          command: bash scripts/ci.sh

workflows:
  build_and_test:
    jobs:
      - build
